import { CreateDevicePipe } from './create-device.pipe';

describe('CreateDevicePipe', () => {
  it('create an instance', () => {
    const pipe = new CreateDevicePipe();
    expect(pipe).toBeTruthy();
  });
});
